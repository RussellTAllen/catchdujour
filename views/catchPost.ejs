<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> One Catch Post, Plz!</h1>
    <% if (user) {%>
        <h2>Logged in User: <%= user.userName %></h2>
    <% } %>
    <h2>Title: <%= catchPost.catchTitle %></h2>
    <h3>Posted by: <%= catchPost.postedBy.userName %></h3>
    <p>Catch Content: <%= catchPost.catchContent %></p>

    <hr>
    <h3>Comments:</h3>
    <% if (catchPost.comments.length > 0) { %>
        <% catchPost.comments.forEach(comment => {%>
            <hr>
            <h4>User <%= comment.user.userName %> said: </h4>
            <p><%= comment.text %></p>
        <% }) %>
    <% } %>

    <% if (user) {%>
        <p>Hey <%= user.userName %>, you can comment and like</p>
        <form action="/catchPosts/comment/<%= catchPost._id %>?_method=PUT" method="POST">
            <label for="text">Enter Comment here: </label>
                <textarea name="text"></textarea>
            <input type="submit">
        </form>
    <% }else{ %>
        <p><a href="/signup">Sign up</a> or <a href="/login">Login</a> to comment</p>
    <% } %>

</body>
</html>